# Task Handoff TB-3002

<details open>
<summary><b>Contents</b></summary>

- [Handoff Identity](#handoff-identity)
- [Completion Summary](#completion-summary)
- [Scope Delivered](#scope-delivered)
- [Changed Files](#changed-files)
- [Contracts and Requirements](#contracts-and-requirements)
- [Validation](#validation)
- [Safety and Security](#safety-and-security)
- [Known Issues and Follow Ups](#known-issues-and-follow-ups)
- [Integration Instructions](#integration-instructions)
- [Reviewer Decision](#reviewer-decision)
- [Coordinator Closure](#coordinator-closure)

</details>

---

**Task ID:** `TB-3002`
**Handoff status:** `SUBMITTED`
**From:** GitHub Copilot (sequential)
**To reviewer:** Unassigned
**Branch:** `main` (sequential; no coordinator appointed)
**Base commit:** `87b5e97`
**Head commit:** Recorded at integration
**Submitted at UTC:** `2026-08-31T14:48:54Z`

## Handoff Identity

| Field | Value |
| --- | --- |
| Task record | [coordination/tasks/TB-3002.md](../tasks/TB-3002.md) |
| Claim record | Not created; `AGENTS.md` permits sequential work when no coordinator is appointed |
| Work package | Three Data |
| Requirement IDs | DATA-001, DATA-003, DATA-004 |
| Requested review decision | `APPROVE` |

## Completion Summary

`src/trading_bot/data/models.py` now holds the canonical market-data values:
`Instrument`, `DailyBar`, `CorporateAction`, and `DatasetManifest`, plus the
`DatasetType`, `ValidationResult`, and `CorporateActionType` enums and the pinned
`CANONICAL_SCHEMA_VERSION`. Field names, types, and optionality follow the
[canonical schema tables](../../docs/04-data-specification.md) exactly.

Every timestamp field is normalized through the TB-1004 `ensure_utc` primitive,
so a naive value cannot enter a canonical row and `session_date`, `ex_date`,
`record_date`, `pay_date`, `effective_start`, and `effective_end` stay
exchange-local dates. A `DailyBar` may not claim `available_at_utc` earlier than
its own `bar_close_utc`, which makes lookahead availability unrepresentable.
Corporate actions must be complete for their own type: a split carries a positive
ratio, a cash dividend carries an amount with exactly one matching currency, and
a symbol change carries two different symbols. The manifest requires sorted and
duplicate-free lineage, lowercase SHA-256 digest shapes, a non-negative row
count, a coherent effective range, no self-parentage, and a canonical sorted
request parameter list that refuses secret-like keys.

The most consequential design choice is what these types deliberately do *not*
enforce. See the boundary note in Contracts and Requirements.

## Scope Delivered

- [x] Every approved subtask is complete.
- [x] Any deferred item has a new linked task ID.
- [x] Work stayed inside the approved write scope.
- [x] Owning tests and documentation were updated.
- [x] No unrelated change or generated artefact is included.

Deviation from approved scope: none.

## Changed Files

| Path | Behavioral or documentation change | Review focus |
| --- | --- | --- |
| `src/trading_bot/data/models.py` | New canonical schemas, enums, and field rules | Rule boundary against `DV-*`, availability guard, secret-key guard, stdlib plus `domain` imports only |
| `src/trading_bot/data/__init__.py` | Exports the new values | Additive surface only |
| `tests/test_data_models.py` | 105 schema and invalid-value tests | One named rejection per rule; enum wire values pinned |

Shared or protected file changes approved by: `None`.
`src/trading_bot/data/__init__.py` is named as shared in the parallelization map
and no competing claim exists.

## Contracts and Requirements

| Requirement or invariant | Implementation evidence | Proposed status effect |
| --- | --- | --- |
| DATA-001 raw and adjusted daily bars for the reference instrument | `DailyBar` carries both series plus provider traceability; `Instrument` fixes internal identity | No status change; ingestion and the provider decision are still open |
| DATA-003 timezone-aware UTC with session identity | Every timestamp field uses `ensure_utc`; naive rejection tests per field; `session_date` stays a `date` | Already `IMPLEMENTED`; this extends the coverage |
| DATA-004 immutable manifest with complete lineage | `DatasetManifest` carries provider, request, schema, calendar, adjustment policy, both hashes, parents, row count, and validation state | No status change; hashing and publication are TB-3006 |
| Architecture dependency direction | `data/models.py` imports only the standard library and `trading_bot.domain.time` | No status change |

**Rule boundary, stated explicitly for review.** The dataset lifecycle places
`NORMALIZED` before `VALIDATING`, and `DV-001`, `DV-002`, `DV-004`, and `DV-005`
must each report *offending canonical rows*. If a canonical row refused to hold
a crossed high and low, a negative volume, or a duplicate key, TB-3005 could not
detect, report, or quarantine those datasets. These types therefore enforce only
representational rules - aware UTC timestamps, canonical identity strings, ISO
4217 currency shape, enum membership, SHA-256 digest shape, sorted lineage, and
finite floats - and leave the twelve validation outcomes to TB-3005. NaN and
infinity are still refused because a validator cannot evaluate an ordering rule
against them. `test_bar_leaves_validation_rule_violations_representable` pins
this boundary so a later change cannot silently break TB-3005.

Decision, migration, schema, or compatibility impact: additive only.
`CANONICAL_SCHEMA_VERSION` is introduced at `"1"`. `DOMAIN_CONTRACT_VERSION` is
untouched because no domain contract changed.

## Validation

| Check | Exact command | Exit code | Result and count | Artefact or log reference |
| --- | --- | --- | --- | --- |
| Focused acceptance | `uv run pytest -q tests/test_data_models.py -k "availability or utc"` | `0` | 10 passed, 95 deselected | Hypothesis in the task record |
| Affected suite | `uv run pytest -q tests/test_data_models.py` | `0` | 105 passed | Component evidence |
| Ruff | `uv run ruff check src tests` | `0` | All checks passed | After fixing one `E501` |
| Strict mypy | `uv run mypy src tests` | `0` | No issues in 28 source files | Frozen slotted dataclasses type-check |
| Full pytest | `uv run pytest -q --cov --cov-report=term-missing` | `0` | 316 passed; 762 statements and 228 branches at 100% | Regression proof |
| Package build | `uv build` | `0` | Wheel contains `trading_bot/data/models.py` | Packaging proof |

Failures encountered and regression proof: the first component run failed
`test_manifest_accepts_derived_lineage_and_an_empty_request`, because the fixture
listed the default `dataset_id` as its own parent and the new self-parentage rule
rejected it; the fixture was given distinct parent identities. Ruff then raised
`E501` on a 105-character test line, which was split. The full suite was rerun
after both fixes.

## Safety and Security

- [x] Current authorization remains `PAPER_ONLY`.
- [x] No real credential, account identifier, private path, or runtime data exists
      in the branch or evidence.
- [x] Missing, stale, ambiguous, retry, restart, and failure behavior was reviewed
      where applicable.
- [x] Risk and reconciliation boundaries were not bypassed or weakened.
- [x] Secret and generated-file checks pass.

Residual safety or security risk: the manifest secret-key guard is a shallow
key-name match, not a redaction engine; it stops an obvious `api_key` parameter
from being persisted into lineage but cannot detect a secret hidden in a value or
an unusual key name. TB-1005 owns the general redaction boundary. No provider
name, endpoint, credential, or network path is introduced by this task; the
example provider strings appear only in test fixtures.

## Known Issues and Follow Ups

| Item | Severity | Blocking | Owner | Follow-up task |
| --- | --- | --- | --- | --- |
| `DV-001` through `DV-012` unimplemented; canonical rows can still be invalid datasets | High | `NO` | Unassigned | TB-3005 |
| Content hashing, canonical serialization, and publication absent, so DATA-004 is partial | Medium | `NO` | Unassigned | TB-3006 |
| `available_at_utc` derivation policy undefined; the schema only forbids lookahead | Medium | `NO` | Unassigned | TB-3004 |
| Universe membership schema not implemented | Low | `NO` | Unassigned | Needs a new coordinator-approved task before cross-sectional research |
| Secret-key guard is key-name only | Low | `NO` | Unassigned | TB-1005 |

Open assumptions or questions: whether `Instrument` should later carry dated
provider-symbol aliases rather than a single current symbol. That is only needed
once symbol-change history is ingested, which is TB-3005 and later work.

## Integration Instructions

| Field | Value |
| --- | --- |
| Required integration order | Before TB-3005 and TB-4003 |
| Expected conflict files | `src/trading_bot/data/__init__.py` if TB-3003 edits exports concurrently |
| Dependency or lockfile action | None; no dependency changed |
| Migration or data action | None; no dataset exists yet |
| Post-integration commands | `uv run ruff check src tests`; `uv run mypy src tests`; `uv run pytest -q`; `uv build` |
| Rollback method | Revert the integration commit; all changes are additive |

## Reviewer Decision

Reviewer appends the decision; do not rewrite worker evidence.

| Field | Value |
| --- | --- |
| Review decision | `TBD` |
| Reviewer | `TBD` |
| Reviewed at UTC | `TBD` |
| Evidence reproduced | `TBD` |
| Required changes | `TBD` |

## Coordinator Closure

| Field | Value |
| --- | --- |
| Final status | `TBD` |
| Integrated commit | `TBD` |
| Master tracker updated | `NO` until completed |
| Traceability updated | `NO` until completed |
| Closed at UTC | `TBD` |
